# 2026-06-06 .NET 10 업그레이드

## 작업 목적

남은 작업인 `.NET 6` 지원 종료 경고 해소를 위해 앱과 테스트 프로젝트의 대상 프레임워크를 `net10.0`으로 업그레이드한다.

## 진행 상태

완료

## 변경 내용

- `BlazorApp_arduinoSearch_240824_01` 대상 프레임워크를 `net10.0`으로 변경했다.
- 서버 앱에서 오래된 `System.Text.Json` 5.0.2 패키지 참조를 제거했다.
- `BlazorApp3` 대상 프레임워크를 `net10.0`으로 변경했다.
- Blazor WebAssembly 패키지를 `10.0.8`로 변경했다.
- 테스트 프로젝트 대상 프레임워크를 `net10.0`으로 변경했다.
- 테스트 프로젝트의 `Microsoft.Extensions.Options` 패키지를 `10.0.0`으로 변경했다.
- CI의 .NET 설치 버전을 `10.0.x`로 정리했다.
- 서버/WASM publish profile의 `net6.0` 경로와 TargetFramework를 `net10.0`으로 변경했다.
- README, 개선 작업 목록, 업그레이드 계획 문서를 `net10.0` 기준으로 갱신했다.

## 검증 결과

| 명령 | 결과 |
| --- | --- |
| `dotnet restore BlazorApp_arduinoSearch_240824_01\BlazorApp_arduinoSearch_240824_01.sln` | 성공 |
| `dotnet restore BlazorApp3\BlazorApp3.sln` | 성공 |
| `dotnet build BlazorApp_arduinoSearch_240824_01\BlazorApp_arduinoSearch_240824_01.sln --configuration Release --no-restore` | 성공, 경고 0개 |
| `dotnet build BlazorApp3\BlazorApp3.sln --configuration Release --no-restore` | 성공, 경고 0개 |
| `dotnet test BlazorApp_arduinoSearch_240824_01\BlazorApp_arduinoSearch_240824_01.sln --configuration Release --no-build` | 성공, 테스트 2개 통과 |

## 중간 이슈와 처리

- 샌드박스에서 restore 시 사용자 `NuGet.Config` 접근 권한이 없어 실패했다.
- 승인 후 restore를 다시 실행해 두 솔루션 모두 성공했다.

## 남은 수동 확인

- 실제 Arduino 장치와 MQTT 브로커가 있는 환경에서 장치 검색, MQTT 설정 전송, LED 제어, 센서 차트 수신을 확인해야 한다.
- Unity WebGL 대시보드는 실제 브라우저에서 로딩과 전체 화면 동작을 확인해야 한다.
